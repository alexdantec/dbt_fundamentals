with payments as (
    
    select 
        id as payment_id,
        orderid as order_id,
        paymentmethod as payment_method,
        status,
        amount,
        created as date_created

    from `steady-shard-347918.dbt_adantec.stripe_payments`
)

SELECT * FROM payments